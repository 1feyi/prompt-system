//import 'package:flutter/material.dart';
//import 'package:prompt_system/provider/courseprovider.dart';
//import 'package:provider/provider.dart';


//class CoursesDisplayPage extends StatelessWidget {
  //final String title;

  //const CoursesDisplayPage({super.key, required this.title});

  //@override
  //Widget build(BuildContext context) {
   // final courses = Provider.of<CourseProvider>(context).courses;

    //return Scaffold(
   //  appBar: AppBar(
     //   title: Text(title),
      //),
   //   body: courses.isEmpty
   //       ? const Center(child: Text('No courses registered.'))
   //       : ListView.builder(
  //            itemCount: courses.length,
  //            itemBuilder: (context, index) {
   //             return ListTile(
   //               title: Text(courses[index]),
  //              );
   //           },
  //          ),
 //   );
//  }
//}
